# Mostly the defaults; only line length is widened to a common 100-col standard
# (long shell commands in configs are folded with `>-` to stay within it).
extends: default

rules:
  line-length:
    max: 100
