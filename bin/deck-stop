#!/usr/bin/env bash
# deck-stop — stop the daemon cleanly (SIGTERM), which triggers the
# "goodbye"/Deck OFF screen before exiting. Use this with KDE autostart.
pkill -TERM -f 'ruby deck.rb run' && echo "stopped." || echo "nothing running."
